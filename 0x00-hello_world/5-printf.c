#include <stdio.h>

/**
 * main - the starting point of a c program
 * return - return integer value 0 aon main
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
